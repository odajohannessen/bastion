namespace Bastion.Data.Domain.DisplaySecret
{
    public class Class
    {
        // TODO: Is own class for displaying secret really needed? 
        // If so, own ID so it can't be traced? Or url id? Can url be gUID?
    }
}
